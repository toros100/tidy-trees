[package]
name = "tidy-trees"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "pptree"

[dependencies]
anyhow = "1"
clap = {version = "4.6", features = ["derive"]}
serde = {version ="1", features = ["derive"]}
serde_json = "1"
